# Archived Bad Outputs

These three Markdown files are the first-run outputs that leaked internal
evidence ids, QA notes, and test comments into user-facing meeting minutes.

They are retained only as QA artifacts. The corrected user-facing files are in
`semantic-clean/` and have also been copied back to the parent `semantic/`
directory with title-synced filenames.
