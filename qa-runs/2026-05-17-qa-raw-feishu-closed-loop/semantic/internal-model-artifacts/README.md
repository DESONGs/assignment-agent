# Internal Model Artifacts

This directory holds model drafts, review notes, and response metadata from the
first QA run. These files are not user-facing meeting minutes and may contain
raw evidence ids or QA review language.

The publishable meeting minutes are the three title-named Markdown files in the
parent `semantic/` directory and the mirrored files in `semantic-clean/`.
